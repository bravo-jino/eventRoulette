{
  "name": "event-roulette",
  "version": "1.0.0",
  "private": true,
  "description": "Static roulette event page",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check js/storage.js && node --check js/app.js && node --check js/admin.js"
  },
  "engines": {
    "node": "24.x"
  }
}
